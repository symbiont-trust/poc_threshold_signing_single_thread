spring:
  profiles:
    active: dev

---

spring:
  config:
    activate:
      on-profile: dev
      
logging:
  level:
    com.thresholdsign: DEBUG
    root: INFO

startup:
    message: "About to run the Threshold Signing Simulation"
    T: 2
    N: 3
    stringToSign: "test"
